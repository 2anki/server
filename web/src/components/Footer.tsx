import styled from "styled-components"

const StyledFooter = styled.footer`
        flex-shrink: 0;
`

const Footer = () => {
        return (
                <StyledFooter>
                        The Footer
                </StyledFooter>
        )        
}

export default Footer